import React from 'react';

interface ModelManagerProps {
  endpointId: string;
}

/** Stub component — endpoint-based model management removed in Copilot SDK migration */
export const ModelManager: React.FC<ModelManagerProps> = () => null;
